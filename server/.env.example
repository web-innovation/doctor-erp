# DocClinic ERP - Environment Configuration

# Server
NODE_ENV=development
PORT=3001

# Database
# Default: SQLite file for local development
# To use SQLite (recommended for local/dev):
DATABASE_URL="file:./prisma/dev.db"

# To use PostgreSQL locally (optional):
# DATABASE_URL="postgresql://user:password@localhost:5432/docclinic?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Google Gemini AI (for image processing)
GEMINI_API_KEY=your-gemini-api-key

# Email Notifications (OAuth2 or SMTP)
# Choose provider: 'gmail', 'outlook', or 'smtp'
# Email Notifications - Choose one provider
# Options: gmail-app-password (easiest), gmail, outlook, smtp
EMAIL_PROVIDER=gmail-app-password
EMAIL_USER=your-email@gmail.com
EMAIL_FROM_NAME=DocClinic

# Gmail App Password (if EMAIL_PROVIDER=gmail-app-password) - RECOMMENDED
# Get from: https://myaccount.google.com/apppasswords
GMAIL_APP_PASSWORD=your-16-character-app-password

# Gmail OAuth2 (if EMAIL_PROVIDER=gmail)
# Setup guide: docs/EMAIL-OAUTH2-SETUP.md
# GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GMAIL_CLIENT_SECRET=your-client-secret
# GMAIL_REFRESH_TOKEN=your-refresh-token

# Outlook OAuth2 (if EMAIL_PROVIDER=outlook)
# OUTLOOK_CLIENT_ID=your-azure-client-id
# OUTLOOK_CLIENT_SECRET=your-azure-client-secret
# OUTLOOK_REFRESH_TOKEN=your-refresh-token
# OUTLOOK_TENANT_ID=your-tenant-id

# SMTP (if EMAIL_PROVIDER=smtp)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_SECURE=false

# WhatsApp Bot API URL
WHATSAPP_BOT_URL=http://localhost:3002

# Twilio WhatsApp API (optional - for automated WhatsApp sending)
# Get credentials from: https://console.twilio.com
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_WHATSAPP_NUMBER=+14155238886  # Twilio sandbox number or your registered number

# AWS SNS SMS configuration
# Set AWS_SMS_ENABLED=true to enable sending via AWS SNS. Set SMS_SANDBOX=true in dev to prevent real sends.
AWS_SMS_ENABLED=false
SMS_SANDBOX=true
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
SMS_SENDER_ID=DOCCLINIC
# Redis (optional) - set to redis://user:pass@host:6379 or leave empty to use in-memory fallback
REDIS_URL=

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Logging
LOG_LEVEL=info
# ===========================================
# HIPAA COMPLIANCE CONFIGURATION
# ===========================================

# Encryption key for PHI (Protected Health Information) - REQUIRED in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HIPAA_ENCRYPTION_KEY=your-32-character-minimum-encryption-key-here

# Hash salt for searchable encrypted fields
HIPAA_HASH_SALT=your-hash-salt-for-phi-data

# Session timeout in minutes (HIPAA recommends 15-30 minutes of inactivity)
SESSION_TIMEOUT_MINUTES=30

# Maximum concurrent sessions per user
MAX_CONCURRENT_SESSIONS=3