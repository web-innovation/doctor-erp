# ==============================================
# DocClinic ERP - Production Environment Template
# IMPORTANT: Never commit actual values to git!
# ==============================================

# Environment
NODE_ENV=production
PORT=3001

# ===========================================
# DATABASE
# This project uses a file-based SQLite database by default for production (`prisma/prod.db`).
# If you prefer RDS PostgreSQL, set `DATABASE_URL` to your connection string.
DATABASE_URL=file:./prisma/prod.db
# Example RDS PostgreSQL:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"

# ===========================================
# SECURITY (HIPAA Compliance)
# ===========================================
# Generate with: openssl rand -hex 64
JWT_SECRET=your-256-bit-secret-here-minimum-64-characters
JWT_EXPIRES_IN=7d

# Generate with: openssl rand -hex 32
HIPAA_ENCRYPTION_KEY=your-64-character-hex-encryption-key

# Generate with: openssl rand -hex 16
HIPAA_HASH_SALT=your-32-character-hash-salt

# Session settings
SESSION_TIMEOUT_MINUTES=30
MAX_CONCURRENT_SESSIONS=3

# ===========================================
# CORS & DOMAIN
# ===========================================
# Your production domain (without trailing slash)
CORS_ORIGIN=https://your-domain.com

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# AWS CONFIGURATION
# ===========================================
AWS_REGION=ap-south-1
# S3 bucket for file uploads (from CloudFormation outputs)
S3_BUCKET=docclinic-production-ACCOUNT_ID

# ===========================================
# EMAIL NOTIFICATIONS (OAuth2 or SMTP)
# ===========================================
# Choose provider: 'gmail', 'outlook', or 'smtp'
EMAIL_PROVIDER=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_FROM_NAME=DocClinic

# Gmail OAuth2 (if EMAIL_PROVIDER=gmail)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-client-secret
GMAIL_REFRESH_TOKEN=your-refresh-token

# Outlook OAuth2 (if EMAIL_PROVIDER=outlook)
# Get credentials from: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
# OUTLOOK_CLIENT_ID=your-azure-client-id
# OUTLOOK_CLIENT_SECRET=your-azure-client-secret
# OUTLOOK_REFRESH_TOKEN=your-refresh-token
# OUTLOOK_TENANT_ID=your-tenant-id

# SMTP (if EMAIL_PROVIDER=smtp, e.g., AWS SES, SendGrid)
# SMTP_HOST=email-smtp.ap-south-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_SECURE=false

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=info

# ===========================================
# FILE UPLOADS
# ===========================================
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads
